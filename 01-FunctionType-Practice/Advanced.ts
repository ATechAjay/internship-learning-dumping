// Advanced Level Tasks
// Define a type for an Invoice object with the following properties:

// id (string), items (array of OrderItem), totalPrice (number).
// Write a function that takes an Invoice object and validates:
// If totalPrice equals the sum of all item prices.
// Return true if valid, otherwise false.
// Implement a function that:

// Accepts an array of OrderItem objects.
// Groups them by category (you’ll need to add a category property to the OrderItem type).
// Returns an object where the keys are categories and the values are arrays of items in that category.
// Create a function with the following requirements:

// It takes an array of objects where each object can be either a Product or an OrderItem.
// The function returns two separate arrays, one containing all Products and the other containing all OrderItems.
